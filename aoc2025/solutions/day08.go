package solutions

import (
	"strings"
)

func Day08PartA(input string, verbose bool) (int, error) {
	_ = strings.Split(input, "\n")
	return 0, nil
}

func Day08PartB(input string, verbose bool) (int, error) {
	_ = strings.Split(input, "\n")
	return 0, nil
}
